//! Inter Display Regular 4.1, embedded by components that need a deterministic
//! Inter font program. See README.md and LICENSE.txt in this directory.

pub const bytes = @embedFile("ttf/InterDisplay-Regular.ttf");
